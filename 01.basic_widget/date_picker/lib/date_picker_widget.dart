import 'package:flutter/material.dart';

class DatePickerDemo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          RaisedButton(
            onPressed: (){

            },
            child: Text("Material DatePciker"),
          ),
        ],
      ),
    );
  }
}
